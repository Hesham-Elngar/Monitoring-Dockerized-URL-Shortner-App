apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: prometheus
  name: prometheus
spec:
  ports:
    - name: "9090"
      port: {{ .Values.service.prometheus.port }}
      targetPort: 9090
  selector:
    io.kompose.service: prometheus
